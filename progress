content hosted on github follwed this
https://medium.com/better-programming/how-to-host-your-react-app-on-github-pages-for-free-919ad201a4cb

use images in publuce 
with <img src={process.env.PUBLIC_URL + 'path'}

CONTENT ADDING

about
contact
nirf
disclosures
dev team
facilities
placement

home
	marquee
	video

https://github.com/Bpitorg/Bpit_Frontend_React.git clone repo
	 

navbar(on collapse change button css so that it is visible)(on hover dropdown must be visible)
footer(link for development team to be added,always at bottom)


var docWidth = document.documentElement.offsetWidth;

[].forEach.call(
  document.querySelectorAll('*'),
  function(el) {
    if (el.offsetWidth > docWidth) {
      console.log(el);
    }
  }
);




token store at local storage (pure js)

tokenn delete at logout(pure js)

when sending request token id mmust be sent (Headers)

new user hits 2 apis to submit

onCLick((DELETE(this.id)))

wmic path softwarelicensingservice get OA3xOriginalProductKey